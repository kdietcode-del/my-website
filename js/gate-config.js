/* ============================================================
   비밀번호 설정

   hash 가 비어 있으면 잠금 없이 바로 열립니다.
   앱의 [설정] → [비밀번호] 에서 비밀번호를 정하면 이 파일을 통째로
   내려받을 수 있습니다. 내려받은 파일로 이 파일을 덮어쓰고 GitHub 에
   올리면 그때부터 잠깁니다.

   ⚠️ 이 파일에 비밀번호 원문은 들어가지 않습니다. 되돌릴 수 없는 형태로
      바꾼 값만 들어갑니다. 다만 정적 사이트의 잠금은 '지나가는 사람'을
      막는 수준이지, 작정하고 들여다보는 사람을 막지는 못합니다.
      README 의 '비밀번호에 대해' 를 읽어 주세요.
   ============================================================ */

const GATE_CONFIG = {
  salt: "",
  hash: "",
  iterations: 200000,
};
